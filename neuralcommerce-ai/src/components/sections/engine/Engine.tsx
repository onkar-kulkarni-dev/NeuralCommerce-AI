import styles from './Engine.module.scss';

const Engine = () => {
    return(
        <section>
        <h2 className={styles.title}>Intelligence Engine at the Core</h2>
        <p className={styles.subTitle}>A central neural processing system that coordinates every transaction, logistics route, and user interaction.</p>
        </section>
    )
}

export default Engine
